/**
 * @file main.c
 * @brief CrossLua Reader entry point — boot sequence and main loop
 *
 * @status Stub — no functionality yet
 * @issues None
 * @todo None
 */

void app_main(void) {
    // Phase 1: Hardware
    // hal_system_init();
    // hal_display_init();
    // hal_gpio_init();
    // hal_storage_init();
    // hal_power_init();

    // Phase 2: Fonts
    // font_loader_init();

    // Phase 3: Lua
    // lua_state_init();
    // lua_api_register_all();

    // Phase 4: Plugins
    // plugin_manager_init();
    // plugin_manager_start();

    // Main loop
    // while (1) {
    //     hal_gpio_poll();
    //     plugin_manager_dispatch_loop();
    //     hal_power_check_sleep();
    //     vTaskDelay(1);
    // }
}
